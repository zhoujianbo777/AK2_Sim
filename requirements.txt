# AK2 PC端仿真软件 + AI模型训练 Python依赖
# 更新日期: 2026-06-04（新增模型训练依赖）
# Python >= 3.10 | PyTorch 2.x | CUDA 12.x

# ============================================================
# 深度学习框架（需手动安装CUDA版本，见下方说明）
# ============================================================
# torch>=2.0.0             # pip install torch --index-url https://download.pytorch.org/whl/cu121
# torchvision>=0.15.0      # pip install torchvision --index-url https://download.pytorch.org/whl/cu121

# ============================================================
# GUI主框架
# ============================================================
PyQt6>=6.6.0

# 高性能实时波形绘制（W2包络、W5弹性波）
pyqtgraph>=0.13.4

# W5 回波3D点云渲染（pyqtgraph.opengl 依赖）
PyOpenGL>=3.1.0

# 静态图表（W3雷达图、W6混淆矩阵热力图）
matplotlib>=3.8.0

# ============================================================
# 数值与科学计算
# ============================================================
numpy>=1.26.0
scipy>=1.12.0

# 配置文件解析
pyyaml>=6.0.1

# 数据文件读写（HDF5格式）
h5py>=3.10.0

# 串口通信（HIL扩展）
pyserial>=3.5

# 备选IPC方案（可选）
pyzmq>=25.1.0

# 进度条（批量评估）
tqdm>=4.66.0

# ─────────────────────────────────────────────────────────────────────────────
# AI 模型开发依赖（参见《AK2 AI模型开发说明》§6.1 及附录C）
# 安装命令（CUDA 12.4）：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# ─────────────────────────────────────────────────────────────────────────────

# 深度学习框架（CUDA版，需单独通过PyTorch官方whl安装）
# torch>=2.x+cu124
# torchvision>=0.x+cu124

# 机器学习工具（标注一致性Kappa / 数据预处理 / 模型评估）
scikit-learn>=1.4.0

# 不平衡学习（SMOTE过采样，文档§5.3）
imbalanced-learn>=0.12.0

# 实验管理（超参数追踪 + 模型版本记录，文档§6.1）
mlflow>=2.13.0

# ONNX 模型导出验证（文档附录C）
onnx>=1.16.0
onnxscript>=0.1.0

# 数据集版本控制（文档附录C）
dvc>=3.50.0

# ============================================================
# 模型训练相关（新增，参考 AK2 AI模型开发说明.md V1.1）
# ============================================================

# 机器学习工具集（标注质量评估Cohen's Kappa、数据划分等）
scikit-learn>=1.3.0

# ONNX 模型导出与验证
onnx>=1.16.0

# 数据处理
pandas>=2.1.0

# 可视化增强（混淆矩阵热力图等）
seaborn>=0.13.0

# 实验管理（超参数追踪、指标记录）
mlflow>=2.14.0
